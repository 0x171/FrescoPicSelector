<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textAllCaps">false</item>
        <item name="toolbarStyle">@style/Base.Widget.AppCompat.Toolbar</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="MaterialTheme" parent="android:Theme.Material.Light.NoActionBar">
        <item name="windowNoTitle">true</item>
        <item name="android:textAllCaps">false</item>
        <item name="toolbarStyle">@style/Base.Widget.AppCompat.Toolbar</item>
        <!-- Customize your theme here. -->
        <!--<item name="android:listDivider">@drawable/album_divider_list_bg</item>-->
        <!--<item name="colorPrimary">@color/appPrimary</item>-->
        <!--<item name="colorPrimaryDark">@color/appPrimary</item>-->
        <!--<item name="colorAccent">@color/appPrimary</item>-->
    </style>

</resources>